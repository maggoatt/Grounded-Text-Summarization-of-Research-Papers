We also observe that the features of NN are more extreme, close to +1 and −1, the limits of the range of the activation function σ (x) = tanh(x). We further note that matrix network NN with square weights has the same final layer shape as the t-NN with cross entropy loss; the only difference between the networks is the first layer. The third network NN( , ) is given the same number of features on both layers as the t-NN. For the t-cross entropy loss, we use a squared ℓ 2 -norm and normalize by the number of entries in the tube. For the t-cross entropy loss, we use a squared ℓ 2 -norm and normalize by the number of entries in the tube.